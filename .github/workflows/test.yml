name: Simple Test Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 23f3004106@ds.study.iitm.ac.in
        run: echo "Hello from GitHub Actions!"

      - name: Done
        run: echo "Workflow completed."
